import java.util.Scanner;
public class Simplify
{
	private String brand;
	private boolean empty;
	public static void main(String [] args)
	{
		Simplify simple = new Simple()
//		Scanner input = new Scanner(System.in);
//		System.out.println("Enter a numerator and a denominator separated by a space: ");
//		int numerator = input.nextInt();
//		int denominator = input.nextInt();
//		if(denominator % numerator == 0)	
//			System.out.println("1 " + denominator / numerator);
//		else if(numerator % denominator == 0)	
//			System.out.println("1 " + numerator / denominator);
//		else
//		{
//			int min = numerator;
//			if(denominator < numerator)
//				min = denominator;
//			int max = denominator;
//			if(min == denominator)
//				max = numerator;
//			boolean found = false;
//			for(int i = max; i >= 1; i--)
//			{
//				if((numerator % i == 0) && (denominator % i == 0))
//				{
//					System.out.println((numerator / i) + " " + (denominator / i));
//					found = true;
//					break;
//				}
//			}
//			if(!found)
//				System.out.println(numerator + " " + denominator);
//		}
	}
}